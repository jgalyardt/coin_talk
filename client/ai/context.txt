I have two issues I need to fix:
1) Requests to api/chat need an Access-Control-Allow-Origin header
2) There's an infinite loop in MarketChart causing "MarketChart.vue:46 RangeError: Maximum call stack size exceeded"

Below are my project files.